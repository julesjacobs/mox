(* Holes cannot be inferred *)
?
> ?206

(* Holes check against any type *)
(? : unit)
> unit

(? : (unit -> unit))
> (unit ->[a=global l=many p=non-portable] unit)

(left(?) : (unit + empty))
> (unit +[u=aliased a=global] empty)

((?, unit) : (unit * unit))
> (unit *[u=aliased a=global] unit)
