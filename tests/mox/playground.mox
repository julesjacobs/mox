unit
> unit

fun x => x
> ('a ->[a=a1 r=r1 l=l1 p=p1] 'a)
> mode vars
>   p1 ∈ {nonportable, portable}
>   l1 ∈ {many, never, once}
>   r1 ∈ [0, inf]
>   a1 ∈ {borrowed, global, local, regional}

(fun x => x) : (unit -> unit)
> (unit ->[a=global r=r1 l=many p=nonportable] unit)
> mode vars
>   r1 ∈ [0, inf]

(fun x => (fun y => x))
> ('c ->[a=a2 r=r2 l=l2 p=p2] ('b ->[a=a1 r=r1 l=l1 p=p1] 'a))
> where
>   'c <=[p=p1 l=l1 r=r1 a=a1] 'a
> mode vars
>   p1 ∈ {nonportable, portable}
>   l1 ∈ {many, never, once}
>   r1 ∈ [0, inf]
>   a1 ∈ {borrowed, global, local, regional}
>   p2 ∈ {nonportable, portable}
>   l2 ∈ {many, never, once}
>   r2 ∈ [0, inf]
>   a2 ∈ {borrowed, global, local, regional}

(fun x => (fun y => y))
> ('b ->[a=a2 r=r2 l=l2 p=p2] ('a ->[a=a1 r=r1 l=l1 p=p1] 'a))
> mode vars
>   p1 ∈ {nonportable, portable}
>   l1 ∈ {many, never, once}
>   r1 ∈ [0, inf]
>   a1 ∈ {borrowed, global, local, regional}
>   p2 ∈ {nonportable, portable}
>   l2 ∈ {many, never, once}
>   r2 ∈ [0, inf]
>   a2 ∈ {borrowed, global, local, regional}

(unit, unit) : (unit *[local unique] unit)
> (unit *[u=unique a=local r=r1] unit)
> mode vars
>   r1 ∈ [0, inf]

(fun p =>
let f = (fun x => x p) in
(f, f))
> ('d ->[a=a4 r=r4 l=l3 p=p3] ((('c ->[a=a2 r=r2 l=l2 p=p2] 'b) ->[a=a1 r=r1 l=l1 p=p1] 'a) *[u=u1 a=a3 r=r3] (('c ->[a=a2 r=r2 l=l2 p=p2] 'b) ->[a=a1 r=r1 l=l1 p=p1] 'a)))
> where
>   'e <= 'c
>   'f <= 'e
>   'g <= 'f
>   'd <=[p=p4 l=many r=r5 a=a5] 'g
>   'h <= 'a
>   'i <= 'h
>   'b <= 'i
> mode vars
>   p1 ∈ {nonportable, portable}
>   l1 ∈ {many, never, once}
>   r1 ∈ [0, inf]
>   a1 ∈ {borrowed, global, local, regional}
>   p2 ∈ {nonportable, portable}
>   l2 ∈ {many, once}
>   r2 ∈ [0, inf]
>   a2 ∈ {borrowed, global, local, regional}
>   r3 ∈ [0, inf]
>   a3 ∈ {borrowed, global, local, regional}
>   u1 ∈ {aliased, unique}
>   p3 ∈ {nonportable, portable}
>   l3 ∈ {many, never, once}
>   r4 ∈ [0, inf]
>   a4 ∈ {borrowed, global, local, regional}
>   a5 ∈ {borrowed, global, local, regional}
>   r5 ∈ [0, inf]
>   p4 ∈ {nonportable, portable}
> mode rels
>   (a5,a3) ∉ {(regional,global), (local,global), (local,regional), (borrowed,global), (borrowed,regional), (borrowed,local)}
>   (a5,a1) ∉ {(regional,global), (local,global), (local,regional), (borrowed,global), (borrowed,regional), (borrowed,local)}
>   (p4,p1) ∉ {(nonportable,portable)}
>   r3-r5 <= 0
>   r1-r5 <= 0
>   r1-r3 ∈ [0, inf]

let x = (unit, unit) in
let y = (x,x) in
let! (a,b) = y in a
> (unit *[u=aliased a=a1 r=r1] unit)
> mode vars
>   r1 ∈ [0, inf]
>   a1 ∈ {borrowed, global, local, regional}

let x = (unit, unit) in
let y = (x,x) in
let! (a,b) = y in 
let! (c,d) = a in c
> error: mode solver detected uniqueness inconsistency
