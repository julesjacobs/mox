unit
> unit

fun x => x
> (?433 ->[a=a1 l=l1 p=p1] ?433)
> mode vars
>   p1 ∈ {non-portable, portable}
>   l1 ∈ {many, never, once}
>   a1 ∈ {global, local, regional}

(fun x => x) : (unit -> unit)
> (unit ->[a=a1 l=l1 p=p1] unit)
> mode vars
>   p1 ∈ {non-portable, portable}
>   l1 ∈ {many, never, once}
>   a1 ∈ {global, local, regional}

(fun x => (fun y => x))
> (?435 ->[a=a2 l=l2 p=p2] (?436 ->[a=a1 l=l1 p=p1] ?437))
> where
>   lock ?435 -> ?437 with a=a1 l=l1 p=p1
> mode vars
>   p1 ∈ {non-portable, portable}
>   l1 ∈ {many, never, once}
>   a1 ∈ {global, local, regional}
>   p2 ∈ {non-portable, portable}
>   l2 ∈ {many, never, once}
>   a2 ∈ {global, local, regional}

(fun x => (fun y => y))
> (?438 ->[a=a2 l=l2 p=p2] (?439 ->[a=a1 l=l1 p=p1] ?439))
> mode vars
>   p1 ∈ {non-portable, portable}
>   l1 ∈ {many, never, once}
>   a1 ∈ {global, local, regional}
>   p2 ∈ {non-portable, portable}
>   l2 ∈ {many, never, once}
>   a2 ∈ {global, local, regional}

(unit, unit) : (unit *[local unique] unit)
> (unit *[u=u1 a=a1] unit)
> mode vars
>   a1 ∈ {global, local, regional}
>   u1 ∈ {aliased, unique}

(fun p =>
let f = (fun x => x p) in
(f, f))
> (?440 ->[a=a3 l=l2 p=p2] ((?448 ->[a=a1 l=l1 p=p1] ?449) *[u=u1 a=a2] (?448 ->[a=a1 l=l1 p=p1] ?449)))
> where
>   lock ?440 -> ?442 with a=a4 l=many p=p3
> mode vars
>   p1 ∈ {non-portable, portable}
>   l1 ∈ {many, never, once}
>   a1 ∈ {global, local, regional}
>   a2 ∈ {global, local, regional}
>   u1 ∈ {aliased, unique}
>   p2 ∈ {non-portable, portable}
>   l2 ∈ {many, never, once}
>   a3 ∈ {global, local, regional}
>   p3 ∈ {non-portable, portable}
>   a4 ∈ {global, local, regional}
> mode rels
>   (a4,a2) ∉ {(regional,global), (local,global), (local,regional)}
>   (a4,a1) ∉ {(regional,global), (local,global), (local,regional)}
>   (p3,p1) ∉ {(non-portable,portable)}
