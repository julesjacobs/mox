let x = (unit, unit) in
let f = (fun a => let! (b,c) = x in b) in
f unit
> unit

let x = (unit, unit) in
let f = (fun a => let! (b,c) = x in b) in
(f unit, f unit)
> error: mode solver detected linearity inconsistency
