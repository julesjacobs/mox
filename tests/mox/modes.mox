(* mode inference on pairs and sums *)
((unit, (unit, unit)) : (unit *[unique global] (unit * unit)))
> (unit *[u=unique a=global r=r2] (unit *[u=aliased a=global r=r1] unit))
> mode vars
>   r1 ∈ [0, inf]
>   r2 ∈ [0, inf]
> mode rels
>   r1-r2 ∈ [0, inf]

(((left(unit)), (right(unit))) : ((unit +[unique global] empty) * (unit +[unique global] unit)))
> error: mode solver detected uniqueness inconsistency

(* arrow annotations roundtrip *)
((fun f => f) : ((unit ->[global once portable] unit) -> (unit ->[global once portable] unit)))
> ((unit ->[a=global r=r3 l=once p=portable] unit) ->[a=global r=r2 l=many p=nonportable] (unit ->[a=global r=r1 l=once p=portable] unit))
> mode vars
>   r1 ∈ [0, inf]
>   r2 ∈ [0, inf]
>   r3 ∈ [0, inf]
> mode rels
>   r1-r3 <= 0

(* redundant mode annotation *)
((unit, unit) : (unit *[global] unit))
> (unit *[u=aliased a=global r=r1] unit)
> mode vars
>   r1 ∈ [0, inf]

(* violation: arrow modes stronger than implementation *)
((fun f => f) : ((unit ->[global once] unit) -> (unit ->[global once portable] unit)))
> error: mode solver detected portability inconsistency
